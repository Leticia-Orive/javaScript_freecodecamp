//Acceda a datos de matriz con índices
/* Podemos acceder a los datos dentro de las matrices usando índices .

Los índices de matriz se escriben en la misma notación de paréntesis que usan las cadenas, excepto que en lugar de especificar un carácter, especifican una entrada en la matriz. Al igual que las cadenas, las matrices usan indexación basada en cero0 , por lo que el primer elemento de una matriz tiene un índice de .


Ejemplo

const array = [50, 60, 70];
console.log(array[0]);
const data = array[1];
La console.log(array[0])imprime 50, y datatiene el valor 60.

Cree una variable llamada myDatay configúrela para que sea igual al primer valor del myArrayuso de la notación de corchetes.

*/
//Ejercicio
/* La variable myData debe ser igual al primer valor de myArray.
myArraySe debe acceder a los datos en variable utilizando la notación de paréntesis.*/
const myArray = [50, 60, 70];
// Only change code below this line.
const myData = myArray[0];
console.log(myData);
